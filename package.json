{
  "name": "listwizard",
  "description": "Manipulate text lists with search/replace, regular expressions, and boolean operators. Convert text lists to CSV files.",
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "svelte-kit dev --port 3213",
    "build": "svelte-kit build",
    "preview": "svelte-kit preview",
    "eslint": "eslint --ignore-path .gitignore .",
    "lint": "standard",
    "fix": "standard --fix"
  },
  "devDependencies": {
    "@sveltejs/kit": "next",
    "eslint": "^7.22.0",
    "eslint-plugin-svelte3": "^3.2.0",
    "svelte": "^3.34.0"
  }
}